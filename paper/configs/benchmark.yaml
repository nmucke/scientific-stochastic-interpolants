defaults:
  - case: stochastic_navier_stokes
  - method: si
  - _self_

results_root: paper/results

# Override at the CLI when sweeping methods, e.g.:
#   python paper/scripts/generate_posterior_samples.py --multirun method=si,fm,flowdas
# To run a sweep of test ids in a single process, set case.test_sample_indices=[0,1,2].
